import { StyleSheet } from "react-native";

const inputStyles = StyleSheet.create({
    input: {
        width: "90%",
        height: 50,
        borderColor: '#ccc',
        borderWidth: 1,
        marginBottom: 15,
        paddingHorizontal: 10,
        borderRadius: 5,
        color: "black"
    }
})

export { inputStyles };
