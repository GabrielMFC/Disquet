import { StyleSheet } from "react-native";

const centralizeFlex = StyleSheet.create({
    containerFlex: {
        display: "flex",
        justifyContent: "center",
        alignItems: "center"
    } 
})

export { centralizeFlex };
